# Phase 1: backend core only.
# Later phases add nnsight, sae-lens, umap-learn, scikit-learn, scipy, runpod, etc.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.4.0
python-multipart>=0.0.9

torch>=2.2.0
transformers>=4.44.0
accelerate>=0.33.0
safetensors>=0.4.0
huggingface-hub>=0.23.0

# Quantization (optional at runtime; CPU users without a CUDA build should skip).
# Kept here so GPU deployments install cleanly by default.
bitsandbytes>=0.43.0; platform_system != "Darwin"

numpy>=1.26.0
aiofiles>=24.1.0
