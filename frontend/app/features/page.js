import PhasePlaceholder from "@/components/common/PhasePlaceholder";

export default function FeaturesPage() {
  return (
    <PhasePlaceholder
      title="SAE feature browser"
      phase="Phase 3"
      description="UMAP-embedded map of sparse autoencoder features, with auto-labeling via Qwen3-0.6B. Depends on SAELens integration."
    />
  );
}
