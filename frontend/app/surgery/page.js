import PhasePlaceholder from "@/components/common/PhasePlaceholder";

export default function SurgeryPage() {
  return (
    <PhasePlaceholder
      title="Weight surgery"
      phase="Phase 2"
      description="Orthogonalize residual-stream-writing weight matrices against the refusal direction. Preview changes live, undo freely."
    />
  );
}
